# Lab 01 · First LLM App — Python deps
# One OpenAI-compatible client covers every backend (local Ollama or hosted Groq/
# OpenAI/etc.) — see provider.py. python-dotenv loads a local .env for API keys.
openai>=1.40
python-dotenv>=1.0
